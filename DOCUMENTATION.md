# تطبيق Teacher Assistant — وثيقة المشروع الكاملة

## نظرة عامة

**Teacher Assistant** هو تطبيق Android حقيقي وقابل للتشغيل، مصمم لإدارة مراكز الدروس الخصوصية والمواليس الخاصة بالمدرسين. يعمل التطبيق بالكامل دون الحاجة إلى اتصال بالإنترنت، ويخزن جميع البيانات في قاعدة بيانات محلية حقيقية على جهاز المستخدم. تم بناء التطبيق بلغة **Kotlin** مع واجهات حديثة باستخدام **Jetpack Compose** و**Material 3**.

## الاختيارات التقنية

نظرًا لعدم توفر ملف مواصفات رسمي، تم اتخاذ الاختيارات التالية بناءً على أفضل الممارسات الصناعية، وهي قابلة للتطوير والتعديل مستقبلًا:

| المكوّن | الاختيار | السبب |
|---------|----------|-------|
| لغة البرمجة | Kotlin | المعيار الرسمي لأندرويد من Google |
| الواجهات | Jetpack Compose + Material 3 | الأحدث والأسرع في التطوير، مع دعم كامل للغة العربية RTL |
| قاعدة البيانات | Room Database (SQLite) | قاعدة بيانات حقيقية وعلاقة على الجهاز، تعمل بدون إنترنت |
| البنية المعمارية | MVVM + Repository + Dependency Injection (Hilt) | البنية المستخدمة في التطبيقات الاحترافية، سهلة الاختبار والتوسع |
| QR Code | ZXing Core 3.5.3 | مكتبة Google المستقرة لتوليد رموز QR |
| النسخ الاحتياطي | تصدير/استيراد JSON كامل (Gson) | ملف واحد قابل للمشاركة والتخزين السحابي |
| الإشعارات/التواصل | تطبيق SMS + إشعارات نظام | الحل الوحيد الفعلي دون خادم خلفي، عبر جهات اتصال ولي الأمر |
| المصادقة | SHA-256 لتمرير المرور + جلسة محلية (SharedPreferences) | بدون خادم، مع إنشاء حساب مدير تلقائي |
| أدوات البناء | Gradle 8.9 + AGP 8.7.3 + Kotlin 2.0.21 | إصدارات حديثة ومستقرة |

## هيكل المشروع

```
app/src/main/java/com/teacherassistant/
├── MainActivity.kt              # النشاط الرئيسي + إدارة الجلسة والتنقل
├── TeacherAssistantApp.kt       # Application class + Hilt
├── data/
│   ├── entity/Entities.kt       # 10 جداول في قاعدة البيانات
│   ├── dao/Daos.kt              # طبقة الوصول للبيانات (10 DAOs)
│   ├── db/AppDatabase.kt        # Room Database + Converters
│   └── repository/AppRepository.kt  # طبقة الأعمال الموحدة
├── di/AppModule.kt              # حقن الاعتمادات (Hilt)
├── viewmodel/                   # 8 ViewModels (منطق كل شاشة)
├── util/
│   ├── SecurityUtils.kt         # التشفير وتوليد رموز QR
│   ├── QrCodeGenerator.kt       # تحويل نص QR إلى صورة Bitmap
│   ├── DateUtils.kt             # أدوات التاريخ الهجري/الميلادي
│   └── BackupManager.kt         # تصدير واستيراد JSON
└── ui/                          # 12 شاشة كاملة (انظر أدناه)
```

## قاعدة البيانات (10 جداول حقيقية)

| الجدول | الوصف |
|--------|-------|
| users | المستخدمون (اسم الدخول، كلمة مرور مشفرة SHA-256، الاسم، الدور، الهاتف) |
| groups | المجموعات الدراسية (الاسم، الاشتراك الشهري، اليوم والوقت) |
| students | الطلاب (الاسم، الهاتف، ولي الأمر، هاتف ولي الأمر، المجموعة، رمز QR، ملاحظات) |
| attendance | الحضور (المجموعة، الطالب، التاريخ، الحالة: حاضر/غائب/متأخر، رقم الحصة، مسح QR) |
| grades | الدرجات (الطالب، العنوان، الدرجة، الدرجة العظمى، التاريخ) |
| recitations | التسميع (الطالب، السورة/الموضوع، النتيجة، التاريخ، ملاحظات) |
| homework | الواجبات (المجموعة، الوصف، تاريخ التسليم) |
| homework_submissions | تسليم الواجبات (الطالب، الواجب، حالة التسليم، الدرجة) |
| payments | المدفوعات (الطالب، المبلغ، النوع: اشتراك/إضافي/خصم، التاريخ، الوصف) |
| notifications | سجل الإشعارات (الطالب، العنوان، الرسالة، النوع، التاريخ) |

## نظام المستخدمين والصلاحيات

- عند أول تشغيل يُنشأ حساب **مدير النظام** تلقائيًا: `admin` / `admin123` (يُفضّل تغيير كلمة المرور فورًا).
- ثلاثة أدوار: **ADMIN** (مدير — صلاحيات كاملة + إدارة المستخدمين)، **TEACHER** (مدرس)، **ASSISTANT** (مساعد — تسجيل حضور ومدفوعات فقط).
- يمكن للمدير إنشاء وتعديل وحذف المستخدمين من شاشة الإعدادات.
- الجلسة محفوظة محليًا، والمستخدم يبقى مسجل الدخول حتى الخروج اليدوي.

## الشاشات (12 شاشة)

| الشاشة | الوظائف |
|--------|---------|
| تسجيل الدخول | تحقق من الحسابات، حفظ الجلسة |
| الرئيسية | إحصائيات سريعة، قائمة جميع الوظائف |
| الحضور والغياب | اختيار المجموعة والتاريخ، تسجيل حضر/غياب/تأخير جماعي أو فردي، حذف سجل اليوم |
| QR الحضور | مسح رمز QR للطالب يدويًا بتسجيل حضور تلقائي، عرض QR لكل طالب |
| الطلاب | إضافة/تعديل/حذف، بحث، توليد رمز QR تلقائي لكل طالب، عرض QR |
| المجموعات | إضافة/تعديل/حذف مجموعات مع الاشتراك الشهري والمواعيد |
| الدرجات والتسميع | إضافة درجات اختبارات، جلسات تسميع مع النتائج، عرض تاريخ الطالب |
| الواجبات | إنشاء واجبات للمجموعة، متابعة تسليم كل طالب (سلم/لم يسلم)، تسليم جماعي |
| المدفوعات | تسجيل دفعات (اشتراك/إضافي/خصم)، **حساب المتبقي تلقائيًا** (الأشهر منذ التسجيل × الاشتراك − المدفوع)، سجل مدفوعات الطالب |
| التقارير | تقرير يومي لكل مجموعة (حاضر/غائب/متأخر + نسبة الحضور)، تقرير طالب (إحصائيات 30 يومًا + المتبقي المالي) |
| الإشعارات | إرسال إشعار لولي الأمر (عبر تطبيق SMS تلقائيًا)، سجل الإشعارات مع حذف |
| النسخ الاحتياطي | تصدير كل البيانات لملف JSON، استيراد مع الدمج |
| الإعدادات | إدارة المستخدمين والأدوار |

## الوظائف الرئيسية بالتفصيل

### الحضور وQR Code
كل طالب يحصل تلقائيًا على رمز QR فريد بصيغة `TA-{معرف الطالب}-{رقم عشوائي}`. عند اختيار "مسح QR" وإدخال الرمز (مثلًا من صورة يرسلها ولي الأمر)، يُسجل حضور الطالب تلقائيًا مع تاريخ اليوم ورقم الحصة، ويُعرض تأكيد فوري.

### حساب المتبقي تلقائيًا
المتبقي = (عدد الأشهر الكاملة منذ تسجيل الطالب × الاشتراك الشهري للمجموعة) − إجمالي المدفوعات. يتم حسابه آليًا في شاشة المدفوعات والتقارير دون تدخل يدوي.

### النسخ الاحتياطي والمزامنة
تصدير جميع الجداول العشرة إلى ملف JSON واحد قابل للمشاركة (WhatsApp/Drive/إيميل)، واستيراد الملف لاحقًا مع الدمج (يُستبدل أي سجل بنفس المعرف).

## كيفية التشغيل

1. انقل ملف `TeacherAssistant.apk` إلى هاتف Android.
2. فعّل "تثبيت التطبيقات من مصادر غير معروفة" في إعدادات الأمان.
3. ثبّت التطبيق وافتحه.
4. سجّل الدخول: اسم المستخدم `admin` وكلمة المرور `admin123`.
5. ابدأ بإنشاء مجموعة، ثم إضافة طلاب، ثم تسجيل الحضور.

## كيفية التطوير مستقبلًا

- المشروع مفتوح كاملًا بصيغة Android Studio القياسية — افتحه مباشرة في Android Studio وأعد البناء.
- لإضافة ميزة جديدة: أضف الجدول في `Entities.kt`، والـ DAO، ثم دالة في `AppRepository.kt`، ثم ViewModel وشاشة.
- للانتقال لاحقًا إلى المزامنة السحابية (عند اشتراك المستخدم في خدمة سحابية): أبقي Room محليًا وأضف طبقة Sync ترفع التغييرات إلى Firebase/Supabase — البنية الحالية معدّة لذلك.

## ملفات المشروع المسلّمة

| الملف | الوصف |
|-------|-------|
| `TeacherAssistant.apk` | حزمة التطبيق الجاهزة للتثبيت (Debug موقّعة — تعمل مباشرة) |
| `TeacherAssistant/` | كامل كود المصدر (مشروع Android Studio كامل قابل للتعديل) |
| `DOCUMENTATION.md` | هذه الوثيقة |

## التعديل في الإصدار 2

- تمت إضافة إدارة مستقلة للصفوف والمراحل الدراسية والمواد.
- تمت إضافة ربط المجموعة بالصف والمادة مع الحفاظ على حقل المادة النصي القديم للتوافق مع البيانات السابقة.
- تمت إضافة Migration من قاعدة البيانات 1 إلى 2 للحفاظ على البيانات الحالية وعدم حذفها عند التحديث.
- تمت إضافة شاشة "الصفوف والمواد" إلى الصفحة الرئيسية، وزر إضافة واضح للمجموعات داخل شاشة المجموعات.
- أصبح تدفق البيانات: الصف/المرحلة ← المادة ← المجموعة ← الطالب، مع استمرار دعم البيانات القديمة.

## ملاحظات مهمة

- **مصدر المتطلبات:** لم يصل ملف المواصفات الفعلي في المحادثة؛ تم بناء التطبيق وفق قائمة المتطلبات الـ 14 المذكورة في الرسائل (حضور، QR، طلاب، درجات، تسميع، واجبات، مدفوعات، تقارير، إشعارات، نسخ احتياطي، مصادقة وصلاحيات). أي تفاصيل إضافية في الملف الأصلي يمكن إضافتها لاحقًا.
- **نوع APK:** النسخة المُسلَّمة هي `debug APK` تعمل على أي هاتف Android 8+، لكنها ليست موقعة بتوقيع Release للنشر على Google Play (يتطلب ذلك keystore خاص بك).
- **الإشعارات لولي الأمر:** دون خادم خلفي، الحل الأضمن هو فتح تطبيق SMS بالرسالة جاهزة لرقم ولي الأمر المحفوظ (مطبّق فعلًا). عند إضافة خدمة إشعارات مدفوعة لاحقًا يمكن الربط مباشرة.
