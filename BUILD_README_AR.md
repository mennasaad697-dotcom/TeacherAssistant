# Teacher Assistant — النسخة المعدلة 1.1.0

هذه النسخة مبنية على مصدر Manus الأصلي، مع إضافة إدارة الصفوف/المراحل والمواد وربطهما بالمجموعات، وإضافة وصول واضح للمجموعات من الصفحة الرئيسية.

## التعديلات
- إضافة جداول `grade_levels` و`subjects` إلى Room.
- ترقية قاعدة البيانات من الإصدار 1 إلى 2 مع Migration للحفاظ على البيانات القديمة.
- إضافة شاشة «الصفوف والمواد» لإضافة/تعديل/حذف الصفوف والمواد.
- إضافة اختيار الصف والمادة عند إنشاء/تعديل المجموعة.
- إضافة «المجموعات» و«الصفوف والمواد» إلى الصفحة الرئيسية.
- إضافة «إضافة سريعة» للطالب والمجموعة والصف/المادة.
- الحفاظ على حقل المادة النصي القديم للتوافق مع البيانات الموجودة.

## البناء
افتح مجلد `TeacherAssistant` في Android Studio، ثم نفّذ Sync Project with Gradle Files وبعدها Build > Build APK(s).

المشروع يستخدم Gradle Wrapper ويستهدف Android 8.0 (API 26) أو أحدث.

## بناء APK من GitHub بدون كمبيوتر
يوجد ملف GitHub Actions داخل `.github/workflows/build-apk.yml`. بعد رفع المشروع إلى GitHub وتشغيل Workflow باسم **Build Android APK**، سيظهر ملف APK في قسم Artifacts باسم `TeacherAssistant-debug-apk`.
